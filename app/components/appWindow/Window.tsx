import React, { useRef } from "react";
type Props = {

}

export default function Window() {
    const position = useRef({ x: 100, y: 100 });

    return (
        <div className="">
        </div>
    )
}
